
  # POS System Development

  This is a code bundle for POS System Development. The original project is available at https://www.figma.com/design/SsB6Y66jTykA8iTEMEycx1/POS-System-Development.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  